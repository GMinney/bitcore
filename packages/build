#!/bin/sh

MODULES='crypto-wallet-core thoughtcore-wallet-service thoughtcore-wallet-client thoughtcore-client'

for i in $MODULES; 
do
  echo "## $i"
  cd $i && npm run compile || (echo "ERROR" && exit) && cd ..
done
